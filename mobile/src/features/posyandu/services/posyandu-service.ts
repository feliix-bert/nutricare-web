import type { PosyanduSession } from '../types/posyandu-types';

export const posyanduService = {
  getSessions: async (): Promise<PosyanduSession[]> => {
    // TODO: implement real API when endpoint is available
    return [];
  },
};
