{
  "name": "gizichain-registry",
  "version": "1.0.0",
  "description": "Transparent Donation & Immutable Health Log System — Hardhat backend",
  "scripts": {
    "compile": "hardhat compile && hardhat run scripts/export-abi.ts",
    "deploy:amoy": "hardhat run scripts/deploy.ts --network amoy",
    "export:abi": "hardhat run scripts/export-abi.ts",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^22.10.0",
    "dotenv": "^16.4.7",
    "hardhat": "^2.22.17",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
